"""lanche = ['Hamburguer', 'Suco', 'Pizza', 'Pudim']"""

# lanche.append('Biscoito') #Ele vai adicionar o biscoito no final da lista
# lanche.insert(0,'Hot-Dog') #Ele vai adicionar a str na primeira lista, e vai chegar todos para o numero +1
# del lanche[3] || lanche.pop(3) ambos vai apagar qual deseja apagar, pop seria mais para usar para apagar o ultimo da lista.
# lanche.remove('Pizza') # vai remover a pizza e vai reposicionar os indices
# lanche.pop() vai eliminar o ultimo elemento

"""

Se for remover uma pizza que já está na lista é só verificar usando o IF

if 'pizza' in lanche:
    lanche.remove('pizza')

"""

# valor = lista(range(4, 11)) #vai fazer uma contagem até 10
# valores = [9, 2, 5, 4, 9, 3, 0] 
# valores.sort = seria o valor em ordem numerais
# valores.sort(reverse=True) = os valores fica em ordem reversa
# len(valores)  = vai ler quantos elementos tem essa lista